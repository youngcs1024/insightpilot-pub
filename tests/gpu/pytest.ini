[pytest]
pythonpath = ../..
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings = error
markers =
    gpu: real GPU inference
    external: authorized target service
addopts = --strict-config --strict-markers
