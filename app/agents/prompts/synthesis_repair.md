# One reference repair

The previous draft had invalid evidence references. Regenerate the complete
SynthesisOutput using only the valid_chunk_ids and exact data generation block
restated with this request. Copy reference values and JSON types exactly; use
only existing rows, columns and statistic fields. All fact claims need supporting
references. Conflict indices must name two different supported claims in this
new output. Do not repeat unsupported numbers. Return no claims if insufficient.
